
<div class="header d-flex justify-content-center p-4 ">
  <h2>Place</h2>
</div>


<div class="container">
  <div class="to_be_booked">
    <div class="image">
      <%= cl_image_tag @place.photo.key, height: 300, width: 400, crop: :fill if @place.photo.key %>

  </div>
  <div class="info_to_be_booked">
    <h2><%= @place.name %></h2>
    <h2><%= @place.price_day.to_i %> €/ day</h2>
    <p><%= @place.description %></p>
    <p><span>Type of place: </span><%= @place.place_type %></p>

    <div class="d-grid gap-2 col-6 mx-auto pt-3">
      <button id="buttonpad">
        <span class="shopping-cart" style="color: white"><i class="fa fa-shopping-cart" aria-hidden="true"></i></span>
        <%=  link_to "Book Now", new_place_booking_path(@place) %>
      </button>
    </div>

  </div>

  </div>

  </div>
